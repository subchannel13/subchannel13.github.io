# subchannel13.github.io

A static web site about me, my skills and my projects.

## Development

```bash
npm install
npm run dev
```

Starts BrowserSync with live reload. The site is available at `http://localhost:3000`.

## Build

```bash
npm run build
```

Generates minified CSS in `dist/style.css`.